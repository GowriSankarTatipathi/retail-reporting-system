# Local development overrides. Loaded when SPRING_PROFILES_ACTIVE=dev (the default).
spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.gowrisankar.retailreporting: DEBUG
    org.hibernate.SQL: DEBUG
    org.springframework.security: DEBUG
