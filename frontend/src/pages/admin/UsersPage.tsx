import { ScaffoldPage } from '@/components/common/ScaffoldPage';

// Real implementation (role/enabled management) lands in the
// "Admin: user management, role management; Profile page" commit.
export default function UsersPage() {
  return <ScaffoldPage title="User administration" />;
}
