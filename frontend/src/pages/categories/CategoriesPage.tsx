import { ScaffoldPage } from '@/components/common/ScaffoldPage';

// Real implementation (CRUD table, search, pagination, sorting) lands in the
// "Categories and Products (+ Inventory) pages" commit.
export default function CategoriesPage() {
  return <ScaffoldPage title="Categories" />;
}
