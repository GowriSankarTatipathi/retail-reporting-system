import { ScaffoldPage } from '@/components/common/ScaffoldPage';

// Real implementation (KPI cards, charts, recent orders) lands in the
// "Dashboard page with KPI cards and charts" commit.
export default function DashboardPage() {
  return <ScaffoldPage title="Dashboard" />;
}
